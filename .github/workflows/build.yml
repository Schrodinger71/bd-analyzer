name: Build and Release BD Scan

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Install Fyne CLI
        run: go install fyne.io/tools/cmd/fyne@latest

      - name: Download deps
        run: go mod tidy

      - name: Package app (LET Fyne build itself)
        run: fyne package -os windows -name "BD Scan" -icon internal/assets/icon.png -app-id com.tulgu.bdscan

      - name: Rename output
        run: rename "BD Scan.exe" "bd-scan-windows.exe"

      - name: Upload release
        uses: softprops/action-gh-release@v1
        with:
          files: bd-scan-windows.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
